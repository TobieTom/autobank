[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = [
  "npm install",
  "npm install --prefix frontend",
  "npm install --prefix agents"
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "npm start"
