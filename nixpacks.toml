[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install --prefix agents"]

[phases.build]
cmds = [
  "npm install --prefix agents",
  "cd agents && (npx tsc || true)"
]

[start]
cmd = "cd agents && node dist/index.js"
